# Prompt Creator

An AI-powered prompt engineering assistant. No backend, no API key — runs anywhere.

Built with React + Vite. Uses [Pollinations.ai](https://pollinations.ai) as a free, open AI API.

## Setup

1. Clone the repo
2. Run `npm install`
3. Run `npm run dev`
4. Open http://localhost:5173